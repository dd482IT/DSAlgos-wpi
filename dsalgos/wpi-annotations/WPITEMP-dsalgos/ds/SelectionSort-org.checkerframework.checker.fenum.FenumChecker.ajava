package ds;

@org.checkerframework.framework.qual.AnnotatedFor("org.checkerframework.checker.fenum.FenumChecker")
public class SelectionSort extends AbstractSort {

    protected void sort(@org.checkerframework.checker.fenum.qual.FenumUnqualified SelectionSort this,  @org.checkerframework.checker.fenum.qual.FenumUnqualified long @org.checkerframework.checker.fenum.qual.FenumUnqualified [] a,  @org.checkerframework.checker.fenum.qual.FenumUnqualified int length) {
        reset();
        for (int out = 0; out < length - 1; ++out) {
            int min = out;
            ++outerLoopCount;
            for (int in = out + 1; in < length; ++in) {
                ++comparisonCount;
                ++innerLoopCount;
                if (a[in] < a[min])
                    min = in;
            }
            if (min != out) {
                swap(a, out, min);
                ++swapCount;
            }
        }
    }
}
