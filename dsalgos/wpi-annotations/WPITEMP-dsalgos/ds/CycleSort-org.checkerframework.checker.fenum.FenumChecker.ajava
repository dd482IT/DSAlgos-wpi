package ds;

@org.checkerframework.framework.qual.AnnotatedFor("org.checkerframework.checker.fenum.FenumChecker")
public class CycleSort extends AbstractSort {

    private  @org.checkerframework.checker.fenum.qual.FenumUnqualified int iterateTillPositioned(@org.checkerframework.checker.fenum.qual.FenumUnqualified CycleSort this,  @org.checkerframework.checker.fenum.qual.FenumUnqualified int cycleStart,  @org.checkerframework.checker.fenum.qual.FenumUnqualified int length,  @org.checkerframework.checker.fenum.qual.FenumUnqualified long item,  @org.checkerframework.checker.fenum.qual.FenumUnqualified long@org.checkerframework.checker.fenum.qual.FenumUnqualified ... a) {
        int pos = cycleStart;
        for (int i = cycleStart + 1; i < length; ++i) {
            ++innerLoopCount;
            ++comparisonCount;
            if (a[i] < item)
                ++pos;
        }
        return pos;
    }

    private  @org.checkerframework.checker.fenum.qual.FenumUnqualified int iterateDuplicates(@org.checkerframework.checker.fenum.qual.FenumUnqualified CycleSort this,  @org.checkerframework.checker.fenum.qual.FenumUnqualified int startPos,  @org.checkerframework.checker.fenum.qual.FenumUnqualified long item,  @org.checkerframework.checker.fenum.qual.FenumUnqualified long@org.checkerframework.checker.fenum.qual.FenumUnqualified ... a) {
        int pos = startPos;
        while (item == a[pos]) {
            ++innerLoopCount;
            ++comparisonCount;
            ++pos;
        }
        return pos;
    }

    private  @org.checkerframework.checker.fenum.qual.FenumUnqualified long swapOutItem(@org.checkerframework.checker.fenum.qual.FenumUnqualified CycleSort this,  @org.checkerframework.checker.fenum.qual.FenumUnqualified long item,  @org.checkerframework.checker.fenum.qual.FenumUnqualified int pos,  @org.checkerframework.checker.fenum.qual.FenumUnqualified long@org.checkerframework.checker.fenum.qual.FenumUnqualified ... a) {
        //    if (item == a[pos]) return item;
        long temp = a[pos];
        a[pos] = item;
        ++copyCount;
        return temp;
    }

    protected void sort(@org.checkerframework.checker.fenum.qual.FenumUnqualified CycleSort this,  @org.checkerframework.checker.fenum.qual.FenumUnqualified long @org.checkerframework.checker.fenum.qual.FenumUnqualified [] a,  @org.checkerframework.checker.fenum.qual.FenumUnqualified int length) {
        if (!shouldSort(length))
            return;
        for (int cycleStart = 0; cycleStart <= length - 2; ++cycleStart) {
            ++outerLoopCount;
            // initialize item as starting point
            long item = a[cycleStart];
            // Find position where we put the item. We basically
            // count all smaller elements on right side of item.
            int pos = iterateTillPositioned(cycleStart, length, item, a);
            // If item is already in correct position
            if (pos == cycleStart)
                continue;
            // ignore all duplicate elements
            pos = iterateDuplicates(pos, item, a);
            ++comparisonCount;
            // put the item to its right position
            item = swapOutItem(item, pos, a);
            // Rotate rest of the cycle
            while (pos != cycleStart) {
                pos = iterateTillPositioned(cycleStart, length, item, a);
                // ignore all duplicate elements
                pos = iterateDuplicates(pos, item, a);
                ++comparisonCount;
                // put the item to its right position
                item = swapOutItem(item, pos, a);
            }
        }
    }
}
